type MedicationData = {
  medicationDataId: string;
  userId: string;
  medicationName: string;
  dosage: string;
  frequency: string;
  note: string;
};

type MedicationDataIdParam = {
  medicationDataId: string;
};
